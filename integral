import numpy as np
import matplotlib.pyplot as plt

def randrange(n, vmin, vmax):
   return (vmax - vmin)*np.random.rand(n) + vmin
   
def f(x):
   return x
   
n=500

x1=randrange(n,0,1)
y1=randrange(n,0,1)
xf=np.array([0,1])

plt.plot(x1,y1,'ro',xf,f(xf),'g-')
plt.show()
